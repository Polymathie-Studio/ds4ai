{
  "name": "beacon-ui",
  "version": "0.1.0",
  "description": "BEACON: findability for shipped pages. A build and SSR-time generator that returns correct <head> metadata (title, description, canonical, Open Graph, Twitter cards) as a string, so it lands in the server HTML where crawlers and social scrapers read it. Framework-agnostic core plus a React binding. In progress toward the full findability set.",
  "keywords": ["seo", "findability", "metadata", "open-graph", "twitter-cards", "head", "ssr", "sitemap", "structured-data"],
  "license": "Apache-2.0",
  "author": "Regis Lloyd Chapman",
  "type": "module",
  "sideEffects": false,
  "main": "beacon.js",
  "exports": {
    ".": "./beacon.js",
    "./react": {
      "types": "./react/index.d.ts",
      "default": "./react/index.js"
    }
  },
  "files": ["beacon.js", "react/", "README.md", "NOTICE"],
  "peerDependencies": {
    "react": ">=18"
  },
  "peerDependenciesMeta": {
    "react": { "optional": true }
  }
}
